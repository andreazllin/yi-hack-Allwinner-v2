# Loaded by `pnpm build:demo` (vite --mode demo), which builds the GitHub Pages
# demo: no camera, no backend, the mock from mock/camera-core.mjs running in a
# Service Worker. This flag is the only switch — the firmware build must never
# set it, or msw ends up in the bundle the camera serves.
VITE_DEMO=1
